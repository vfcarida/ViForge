method:
  type: "sft"
  learning_rate: 1.0e-4
  lr_scheduler: "cosine"
  warmup_ratio: 0.03
  num_epochs: 3
  max_seq_len: 4096
  per_device_batch_size: 2
  gradient_accumulation_steps: 8
  gradient_checkpointing: true
