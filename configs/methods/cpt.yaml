method:
  type: "cpt"
  learning_rate: 5.0e-5
  lr_scheduler: "cosine"
  warmup_ratio: 0.03
  num_epochs: 1
  max_seq_len: 4096
  per_device_batch_size: 2
  gradient_accumulation_steps: 8
  gradient_checkpointing: true
